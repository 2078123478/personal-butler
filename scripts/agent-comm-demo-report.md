# Agent-Comm Demo Report Notes

The default walkthrough has been updated to the v2 contact-first flow:
- export/import signed cards
- invite/accept to establish trust
- trusted business send with `contact:<contactId>`

Use `scripts/agent-comm-demo.sh` and `scripts/agent-comm-demo.md` as the current source of truth.

If direct-tx sends fail during the demo, the most common cause is missing gas on the active comm wallet.
